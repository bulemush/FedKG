# FedKSA：面向大语言模型联邦离线调优的结构感知知识自适应方法

## 中文检查稿说明

本文档是根据当前英文 `elsarticle` 论文草稿整理的中文检查稿，用于检查论文逻辑、创新点、方法叙述和实验占位是否合理。它不是投稿用中文论文，也不是中文 LaTeX 编译稿。公式、引用和表格内容已转写为便于阅读的中文形式，实验数值仍保留为待填写占位。

## 摘要

知识密集型语言应用通常依赖本地实体、关系、规则以及证据图，而这些知识往往无法在不同机构或设备之间集中汇聚。联邦调优提供了一种隐私保护的训练协议，但直接调优大语言模型，或者同步大规模知识增强模块，仍会给存储、显存和通信能力有限的客户端带来较高成本。本文提出 FedKSA，一种面向大语言模型联邦离线调优的结构感知知识自适应框架。

FedKSA 的核心思想是将结构化证据表示为适配器侧的可训练知识增量：在离线调优中，模拟器—适配器边界决定了客户端可见、可训练和可通信的模型区域，因此结构化知识自适应也应被组织在这一边界之内。基于这一原则，FedKSA 通过知识感知的混合嵌入将本地图证据与文本 token 表征对齐，在适配器侧层中执行图推理、三元组推理和 token—图联合推理，并在服务器端仅聚合可训练模型状态。本文为 WebQSP、ComplexWebQuestions 以及 OpenBookQA/ConceptNet 准备了评测协议，基线包括集中式 LoRA、联邦 LoRA、仅语言的离线调优以及关键组件消融。当前草稿为已验证实验结果预留了表格，同时给出了面向 Knowledge-Based Systems 投稿所需的方法、报告协议和分析结构。

## 图文摘要占位

本地证据图 → 适配器侧结构化知识增量 → 联邦可训练状态聚合。

## Highlights

- FedKSA 在联邦离线大语言模型调优中自适应本地图结构知识。
- 结构化证据被建模为适配器侧的可训练知识增量。
- 图推理、三元组推理和 token—图推理被统一到适配器层内部。
- 当前草稿为经过验证的实验结果保留了明确的表格填充空间。

## 关键词

联邦学习；大语言模型；离线调优；知识图谱；知识自适应；问答

## 1. 引言

知识密集型人工智能系统正在越来越多地将自然语言交互与结构化证据结合起来。领域助手、机构级问答服务或科学决策支持系统都可能需要回答流畅的自然语言问题，但其答案往往依赖显式实体、关系、多跳路径和本地约束。这些知识源通常不会以一个干净、集中的语料库形式存在，而是分布在不同机构、业务单元、实验室或用户设备中，并可能包含隐私记录、专有知识库以及本地人工维护的图片段。

这种部署模式在模型能力和数据治理之间形成了张力。大语言模型为指令跟随和自然语言生成提供了灵活接口，但知识密集型推理又受益于图结构证据。联邦学习通过将原始数据保留在客户端、仅在服务器端聚合模型更新，部分缓解了数据治理问题。然而，将传统联邦调优直接应用于大语言模型成本较高，因为客户端可能无法存储、更新或传输完整大模型相关的状态。当任务还需要结构化知识模块时，问题会进一步复杂化，因为这些模块可能引入额外的实体表、图编码器和关系特定参数。

现有方法分别解决了这一问题的不同侧面。参数高效微调减少了需要训练的大语言模型参数数量，LoRA 是其中具有代表性的方法。离线调优进一步降低客户端需求，通过使用轻量模拟器替代完整模型的一部分，并将适配器侧区域暴露给客户端。知识增强语言模型则通过实体嵌入、检索、图神经网络或关系感知注意力机制来提升事实推理和多跳推理能力。这些方向本身是有价值的，但它们并没有明确回答一个关键问题：在联邦离线大语言模型中，结构化知识应该被放置在模型的什么位置。

现有联邦大语言模型调优方法通常将客户端知识视为纯文本监督。当本地证据本质上是图结构时，将其序列化为提示词，或仅依赖普通语言建模，可能会削弱实体对齐、关系路径和组合约束。模型可能从生成目标字符串中学习，却没有维护一个与本地证据图对应的持久结构状态。这会削弱知识表示与跨客户端同步的可训练参数之间的联系。

现有知识增强大语言模型方法则通常假设可以集中访问图数据，或可以不受限制地访问完整模型。许多图增强模型是在训练器能够同时看到图、文本和完整模型参数的场景中设计的。然而，在联邦离线调优中，这一假设不成立：每个客户端只拥有自己的本地样本和子图，服务器不应接收原始图或由图派生出的样本级记录。因此，依赖集中式实体表或完整模型更新的知识模块并不适合这一部署场景。

现有离线调优流程虽然定义了客户端可见的适配器区域，但尚未为这一区域定义结构化知识自适应原则。模拟器—适配器边界已经区分了客户端可以训练和通信的模型部分，以及冻结或隐藏的骨干模型部分。如果没有相应的知识建模设计，结构化证据就会停留在主优化边界之外，只能作为辅助输入，而不是模型级的自适应对象。

基于上述观察，本文提出 FedKSA，一种结构感知知识自适应框架，将本地图证据表示为面向联邦离线大语言模型调优的适配器侧可训练知识增量。FedKSA 的核心原则是：在联邦离线调优中，模拟器—适配器边界决定了客户端可见、可训练和可通信的区域；因此，结构化知识自适应应当被组织在该区域内部。这一原则将本地子图转化为一个有界的模型增量，而不是一个松散附加的图模块。

图 1 展示了 FedKSA 的整体思路。左侧为仅语言的联邦离线调优，它暴露模拟器和适配器侧层，但只使用本地文本监督。右侧为 FedKSA，它将每个本地子图映射为适配器侧结构状态，在适配器可见层中执行图推理、三元组推理和 token—图推理，并且只上传可训练状态。

本文贡献概括如下：

1. 适配器侧知识自适应原则。本文将离线调优中的模拟器—适配器边界形式化为联邦结构化知识自适应的建模边界，从而避免将图证据仅视为提示层面的补充，而是使其成为客户端侧可训练增量的一部分。

2. 多粒度结构推理机制。本文设计了一条统一路径，结合知识感知混合嵌入、图消息传递、三元组编码以及 token—图联合推理。这些组件不是彼此孤立的附加模块，而是在同一个适配器侧知识状态上的连续操作。

3. 联邦可训练状态同步协议。本文通过统一的可训练状态接口对齐语言自适应、结构推理和服务器聚合。服务器聚合模型增量，而原始文本样本和样本级图始终保留在客户端。

4. 知识密集型评测协议。本文为 WebQSP、ComplexWebQuestions 和 OpenBookQA/ConceptNet 准备了实验设计，并为主结果、消融实验和效率分析预留了结果占位。最终投稿前应使用经过验证的日志填充这些结果。

## 2. 相关工作

### 2.1 联邦调优与大语言模型参数高效微调

联邦学习在不向服务器收集原始数据的前提下，在分布式客户端上训练模型。对于大语言模型而言，核心困难不仅是统计异质性，还包括模型状态规模过大。参数高效微调方法减少了预训练模型中需要训练的部分，LoRA 是一种代表性方法，它通过低秩矩阵参数化模型更新。这些方法能够降低通信和存储压力，但并不能完全消除客户端与大型模型组件交互的需求。同时，它们也没有定义当每个客户端拥有不同图片段时，本地结构化证据应如何表示。

### 2.2 离线调优与有限访问模型自适应

离线调优通过将大语言模型分解为模拟器和适配器侧区域，解决有限访问场景下的模型自适应问题。模拟器近似原始模型的一部分，而适配器区域由用户或客户端训练。这种分解对于联邦大语言模型调优具有吸引力，因为它在隐藏或冻结的模型组件与可本地优化的状态之间形成了自然边界。FedKSA 建立在这一边界之上，但改变了适配器侧的角色：适配器不仅是语言自适应区域，也是本地结构化证据被转化为可训练知识增量的位置。

### 2.3 知识增强语言模型

知识增强语言模型通过实体表示、图神经网络、关系感知注意力、检索或重排序等方式引入外部知识。当相关证据可用时，这些方法可以改善事实推理和多跳问答。然而，许多知识增强语言模型假设可以集中访问图、训练数据或完整模型。在联邦离线场景中，这一假设过强。客户端可能只允许暴露参数更新，而服务器不应接收样本级子图。因此，FedKSA 关注的是本地图证据与适配器侧可训练状态之间的接口。

### 2.4 知识密集型问答

知识密集型问答基准为结构感知自适应提供了有价值的测试场景。WebQSP 评估基于 Freebase 风格语义解析的问题。ComplexWebQuestions 强调组合式问题，其答案通常依赖多条关系和约束。OpenBookQA 评估常识多选推理，而 ConceptNet 提供了广泛的常识图，可用于构造问题概念与候选答案之间的证据路径。这些数据集适合评估 FedKSA，因为每个样本都可以表示为文本与本地证据图的配对形式，恰好对应本文提出的适配器侧知识自适应接口。

## 3. 问题定义

考虑一个由一个服务器和 K 个客户端组成的联邦系统。客户端 k 持有私有本地数据集，其中每个样本由输入文本、目标答案或生成序列，以及样本级证据图组成。证据图可以包含实体或概念节点、关系边、节点类型、边类型、节点文本描述以及 token—实体对齐信息。原始样本和原始图始终保留在客户端。

服务器初始化一个预训练大语言模型。在离线调优中，一段连续的 Transformer 层由模拟器近似，而其余可见区域构成适配器侧模块。本文将客户端可见的离线模型写作左侧适配器、模拟器和右侧适配器的组合。若 l 和 r 分别表示模拟器区域的左右边界，则适配器侧层集合由模拟器之前的层和模拟器之后的层组成。这个集合非常重要，因为它定义了在离线约束下客户端可以执行可训练自适应的位置。

FedKSA 在这一适配器侧区域内部引入图条件化自适应函数。模型预测不仅依赖输入文本，也依赖由本地证据图诱导出的结构化自适应状态。训练目标主要是语言建模损失，即在给定输入文本、本地图证据和先前目标 token 的条件下最大化目标序列概率。当启用模拟器对齐时，还可以优化模拟器状态与教师块状态之间的对齐项，包括表示相似性损失和教师—学生分布之间的 KL 散度。局部目标在启用对齐时为语言建模损失加对齐损失，否则仅为语言建模损失。

在第 t 轮通信完成本地更新后，每个客户端只上传自己的可训练状态。服务器按照客户端样本数量进行加权聚合，得到下一轮全局可训练状态。该形式化区分了三个常被混淆的对象：原始证据图、本地图条件化状态，以及同步的可训练参数。FedKSA 只通信第三类对象，而不通信前两类对象。

表 1 给出了主要符号说明。K 表示客户端数量；本地数据集表示客户端 k 的私有数据；每个样本由文本输入、目标输出和本地证据图组成；预训练大语言模型由服务器初始化；模拟器用于近似压缩后的 Transformer 区域；左右适配器表示客户端可见的适配器侧区域；适配器侧层集合用于结构感知自适应；可训练参数包含语言与结构自适应参数；图条件化结构状态表示由本地证据图诱导出的适配状态。

## 4. FedKSA 框架

### 4.1 总体框架

FedKSA 面向一种更贴近真实部署的联邦知识密集型场景：每个客户端都拥有私有文本样本和本地证据图，而服务器只能协调可训练模型状态，不能集中收集原始文本、原始图或样本级证据。基于这一约束，FedKSA 的核心原则是：在离线调优中，模拟器—适配器边界决定了客户端可见、可本地优化、可跨客户端同步的模型区域，因此结构化知识不应作为外部提示补充或独立图模块附着在模型之外，而应被转化为遵循同一边界的适配器侧知识增量。

对于本地样本，输入文本由离线大语言模型处理，本地证据图则被转换为仅在当前批次中激活的紧凑结构状态。证据图由节点标识、关系标识、类型化边、可选三元组、文本描述以及 token—实体对齐组成。节点表示实体或概念，边表示实体间关系，三元组保留局部事实结构，token—实体对齐用于建立文本位置与图节点之间的联系。若样本没有可用证据图，则映射为空图，使同一架构能够兼容知识增强样本和纯语言样本。

FedKSA 的前向过程包含三个耦合变换。首先，知识感知混合嵌入将离散标识和文本描述结合起来，既保留本地实体的稳定身份，又利用语言语义缓解稀疏节点问题。其次，结构感知推理只在适配器侧层集合中执行，使图消息、三元组上下文和 token 状态在客户端可训练区域内联合更新。最后，客户端基于语言建模目标和可选的模拟器对齐目标进行本地优化，并仅上传被允许同步的可训练参数。

因此，FedKSA 明确区分了三类对象：原始证据图、样本级结构状态和联邦同步的可训练参数。前两者始终留在客户端，只有第三类对象参与服务器聚合。这种设计使知识路径从 token—实体对齐进入模型，在适配器侧结构推理中演化，并最终只以适配器参数更新的形式离开客户端，从而避免依赖集中式图访问、完整模型更新或样本级证据共享。

### 4.2 知识感知混合嵌入

知识密集型任务要求模型同时识别符号化图元素及其语言语义。单纯使用实体标识嵌入虽然稳定，但对于长尾实体、客户端私有实体或稀疏概念可能缺乏语义信息；单纯依赖文本描述嵌入虽然包含语义，但当描述过短、噪声较多或缺失时又不够可靠。为此，FedKSA 采用知识感知混合嵌入，在节点和关系进入大语言模型 token 流之前，为其构造兼具符号稳定性和语义可解释性的初始状态。

对于节点，FedKSA 同时构造两类表示。第一类是标识符嵌入，用于表示实体或概念在本地图词表中的离散身份；第二类是文本表示，通过池化节点描述对应的 token 表征，并将其投影到知识隐藏空间得到。随后，模型使用门控机制在两类表示之间自适应加权：当节点描述缺失或不可靠时，更依赖标识符嵌入；当文本描述包含有效语义时，更充分利用语言派生表示。关系状态采用相同思想，由关系标识和关系文本共同初始化。

混合图状态随后通过 token—实体对齐注入 token 流。给定对齐矩阵，模型为每个相关 token 位置收集或注意力汇聚对应节点状态，形成图到 token 的结构增量。该结构增量通过门控残差路径加入原始 token 表征，并经过层归一化处理。残差形式非常关键：它为本地结构证据提供了明确入口，同时避免图信号直接覆盖预训练语言表示。当一个 token span 对齐到多个节点时，模型通过注意力池化选择相关节点；当没有对齐节点时，结构增量被掩码为零。

这一嵌入层的作用不只是“输入增强”。它为不同数据源提供了统一的图输入接口，使 WebQSP、ComplexWebQuestions 和 OpenBookQA/ConceptNet 虽然具有不同的图来源和关系词表，仍能被映射为同一种模型可处理的结构状态。同时，它也保证后续结构推理是客户端本地和实例感知的：FedKSA 不需要服务器维护集中式实体表，而是由每个客户端基于本地标识、描述和对齐信息构建图状态。

### 4.3 适配器侧图推理与三元组推理

在初始图信号进入 token 流之后，FedKSA 只在适配器侧层集合中执行结构推理。这一位置选择是方法成立的关键。在离线调优中，模拟器区域主要用于近似隐藏或冻结的模型块，并不适合承载客户端特定知识更新；相反，适配器侧层是客户端可见且可训练的区域，因此是将本地图证据转化为可影响预测、并可通过参数更新被联邦聚合反映出来的自然位置。

在一个增强的适配器侧层中，Transformer 计算首先更新 token 状态，随后结构感知模块读取当前批次中的节点状态、关系状态、边集合和三元组状态。对于每个节点，关系感知图传播从邻居节点聚合消息，消息由邻居节点表示和边上的关系表示共同决定，并通过归一化边权或注意力系数加权。节点状态随后通过门控残差变换更新。这样，局部关系路径和图邻域能够细化实体状态，而原始图本身不需要暴露给服务器。

图邻域能够捕捉关系上下文，但许多知识密集型问题依赖具体事实。为此，FedKSA 进一步引入三元组粒度的事实编码。对于给定三元组，模型将头实体、关系和尾实体编码为三元组上下文。该表示保留了局部事实的组合结构，能够补充图传播中可能被平滑掉的精确关系信息，尤其适用于答案依赖某一具体事实而非整体邻域语义的情况。

更新后的节点状态和三元组状态通过 token 到结构的注意力机制融合回文本表示。token 状态作为查询，节点状态和三元组状态作为键和值，使语言路径能够选择当前生成或分类所需的实体、关系和事实。该融合仍采用残差形式，并可使用可学习或调度的融合系数，以保持预训练 token 动态的稳定性。由于这种交互会在多个适配器侧层中重复进行，FedKSA 形成了迭代式文本—图推理过程：token 状态选择有用结构证据，图状态通过关系传播得到细化，细化后的结构再返回语言路径。

运行时图上下文也被纳入这一小节的机制中。它只在当前前向传播中保存图张量、掩码、对齐、节点状态、关系状态和三元组状态，并在前向传播结束后清空。这避免了样本级图信息被持久写入全局模型，也防止图证据成为通信对象。如果某个批次没有证据图，则所有图掩码均处于非激活状态，结构模块退化为空操作，模型仍可执行普通的仅语言离线调优。

### 4.4 联邦微调

FedKSA 的训练协议与上述架构边界保持一致。每个客户端接收当前全局可训练状态，使用自己的私有文本—图批次执行本地离线调优，并且只上传被选择的适配器侧可训练参数。冻结骨干模型、模拟器、原始文本样本、原始图以及样本级运行时结构状态均不上传。

对于本地样本，模型预测由输入文本和图诱导的适配器状态共同决定。可训练参数包括语言适配器和结构适配器参数，而图诱导状态由本地证据图生成。主要本地目标是序列建模损失，即在给定输入文本、本地图证据和历史目标 token 的条件下最大化目标序列概率。当启用模拟器对齐时，还可以加入表示相似性损失和教师—学生分布之间的 KL 散度，使基于模拟器的计算接近对应教师块行为。最终本地目标在启用对齐时为语言建模损失加对齐损失，否则仅为语言建模损失。

在每一轮通信中，客户端完成本地优化后上传选定的可训练状态，服务器按照客户端样本数量执行加权聚合。实际实现中，同步白名单可以限制为低秩语言适配器、结构投影矩阵、结构门控以及轻量图/三元组推理参数。这样既能控制通信成本，也能缩小客户端特定知识的暴露面。不过，这种限制本身不应被表述为形式化隐私保证；在隐私敏感部署中，还应结合安全聚合、差分隐私或梯度裁剪。

从整体上看，FedKSA 的联邦微调协议解决的是本文的核心部署问题：客户端能够利用本地结构化证据对大语言模型进行知识密集型适配，而联邦系统只通信有界的可训练状态，不通信原始图、完整模型参数或样本级证据记录。

## 5. 实验设置与补全计划

本节被有意写成可补全的实验章节。表格目前可以在 Overleaf 中编译，数值可在完成并验证相应实验后填入。

### 5.1 数据集

WebQSP 评估基于 Freebase 风格实体和关系的知识库问答。每个样本可关联到以问题为中心的图，其中包含主题实体、关系链、约束和答案变量。

ComplexWebQuestions 评估组合式和多跳问答。其 SPARQL 结构可以自然提供本地关系图，包括实体、变量和答案节点。

OpenBookQA/ConceptNet 评估常识多选推理。ConceptNet 子图连接问题概念和候选选项，为答案选择提供结构化常识证据。

### 5.2 基线方法

计划中的基线用于隔离离线边界的作用和结构化知识的作用。Central-LoRA 作为没有联邦隐私约束的集中式上界参考。FedAvg-LoRA 使用联邦参数高效调优，但不使用离线分解或结构自适应。Offsite-FT 使用联邦离线调优，但关闭图条件化自适应。其余变体移除 FedKSA 的单个组件，包括图消息传递、三元组编码、token—图联合推理以及混合图—token 嵌入。

### 5.3 实现细节

主要配置使用 Llama-2-7B 骨干模型，LoRA 秩为 8，LoRA 缩放因子为 16，dropout 为 0.05。联邦设置使用 3 个客户端、200 轮通信，以及每轮 30 个本地更新步。离线调优使用模拟器区域，其中 emu_l 为 0，emu_r 为 30，drop-layer 比率为 0.2。结构适配器使用 256 的隐藏维度、4 个注意力头、关系感知图传播、三元组推理和 token—图联合推理。

当前项目包含 WebQSP、CWQ 和 OpenBookQA/ConceptNet 的可运行配置文件。KGQA 评估脚本报告 hit@1，支持精确匹配或包含式匹配；MCQA 评估脚本支持基于生成和基于选项打分的评测。最终论文应为每个表格项记录命令行、随机种子、检查点、结果文件和硬件细节。

### 5.4 主结果

主结果表需要报告 WebQSP hit@1、CWQ hit@1、OpenBookQA 准确率、可训练参数量以及每轮上传量。方法包括 Central-LoRA、FedAvg-LoRA、Offsite-FT 和 FedKSA。当前所有数值均为待填充占位。

结果可用后，第一段分析应回答适配器侧结构自适应是否相较仅语言的联邦离线调优提升了知识密集型推理。第二段应讨论增益是否在 KGQA 和常识 MCQA 中一致。第三段应将性能提升与通信成本联系起来。

### 5.5 消融实验

消融表需要比较 FedKSA 去除混合嵌入、去除 GNN、去除三元组、去除联合推理以及完整模型时的 WebQSP hit@1 和 OpenBookQA 准确率。

消融分析不应将这些组件描述为彼此独立的附加模块，而应解释每次移除削弱了适配器侧知识增量的哪个部分。移除混合嵌入会削弱图—token 入口。移除图传播会削弱关系路径 refinement。移除三元组会削弱局部事实组合。移除联合推理会削弱文本状态与结构状态之间的交换。

### 5.6 效率与通信

效率表需要报告 FedAvg-LoRA、Offsite-FT 和 FedKSA 的可训练参数量、每轮上传量、轮次时间和峰值显存。最终版本应同时报告准确率指标和系统指标。若没有参数量和上传大小，论文只能主张结构建模贡献，而不能充分主张效率贡献。

### 5.7 定性分析

实验完成后，建议至少包含一个成功案例、一个被 FedKSA 修正的基线失败案例，以及一个 FedKSA 失败案例。每个案例应展示问题、本地证据图的紧凑视图、基线输出、FedKSA 输出，并简要说明哪条图关系或哪个三元组起到了关键作用。

## 6. 讨论

### 6.1 知识自适应的统一边界

FedKSA 的主要设计价值在于它施加的边界。该框架并不是简单地将图编码器附加到联邦调优器上，而是首先提出一个问题：在联邦离线约束下，知识可以被表示在模型的什么位置？FedKSA 的回答是：表示在适配器侧区域内部。由此，该方法获得了一个一致的建模对象，即适配器侧结构化知识增量。

### 6.2 通信与隐私

FedKSA 将原始文本样本和样本级图保留在本地。服务器聚合的是可训练状态，而不是原始证据。这种设计与通信受限的联邦学习兼容，因为上传对象可以被限制为适配器参数、低秩矩阵和选定门控。同时，参数更新仍可能泄露信息。面向实际部署的版本应将 FedKSA 与安全聚合、差分隐私、梯度裁剪或更严格的参数白名单结合起来。

### 6.3 局限性

当前草稿尚未包含完整数值结果。在所有主比较和消融实验被验证之前，最终论文不应作出优越性结论。该方法也可能对图质量敏感：WebQSP 和 CWQ 依赖语义解析质量，而 OpenBookQA 依赖 ConceptNet 检索和剪枝质量。最后，如果结构模块没有采用低秩化或选择性同步，也可能增加可训练参数量。这些局限性正是实验章节中效率表和消融表需要补充的原因。

## 7. 结论

本文提出 FedKSA，一种面向大语言模型联邦离线调优的结构感知知识自适应框架。该方法建立在一个原则之上：结构化知识应被表示在离线模拟器—适配器边界所定义的适配器侧区域内部。在这一原则下，本地证据图被转化为可训练知识增量，图推理和三元组推理在适配器可见层中运行，服务器仅聚合可训练状态。当前草稿提供了一篇可在 Overleaf 中编译的方法论文，并为 WebQSP、ComplexWebQuestions 和 OpenBookQA/ConceptNet 上经过验证的实验预留了明确空间。未来工作应完成实验表格，进一步收紧结构参数的低秩同步设计，并在真实分布式知识系统中评估隐私保护聚合机制。

## 附录 A. 额外实现细节

### A.1 配置检查清单

最终实验附录应包含精确配置路径、随机种子、检查点路径和评估命令。当前项目计划识别了以下配置族：带 KG 自适应的 WebQSP、带 KG 自适应的 CWQ、不带 KG 自适应的 CWQ，以及带 KG 自适应的 OpenBookQA/ConceptNet。

### A.2 结果补全清单

投稿前，每个数值表都应能追溯到一个结果产物，其中包含命令、随机种子、数据集划分、检查点、指标定义和输出文件。KGQA 应在选定匹配模式下报告 hit@1。MCQA 应报告准确率，并说明答案来自生成还是选项打分。

### A.3 引用核对清单

当前草稿中的 BibTeX 文件有意保持精简。投稿前，每条引用都应根据 DBLP、ACL Anthology、OpenReview、arXiv 或出版商元数据进行核查。当存在已发表版本时，应使用已发表版本替换不完整的预印本条目。

## 检查时建议重点关注的问题

1. 创新靶点是否清楚：本文创新不是“联邦调优加图模块”，而是将联邦离线调优重新定义为适配器侧结构化知识自适应问题。

2. 方法边界是否统一：混合嵌入、图推理、三元组推理、运行时上下文和联邦聚合都应服务于“适配器侧知识增量”这一对象。

3. 实验论证是否可补全：后续实验应优先证明 FedKSA 相比 Offsite-FT 的增益，其次证明相比 FedAvg-LoRA 的隐私/通信优势，最后通过消融证明结构路径的必要性。

4. 表述风险是否可控：在实验结果未补齐前，不应使用“显著优于”“大幅提升”等确定性措辞，只能写作“用于验证”“预期分析”“结果补全后讨论”。
